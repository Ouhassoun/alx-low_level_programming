#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @nb: fonction's parameter
 * Return: integer
 */

int _abs(int nb)
{

	if (nb < 0)
		return (-nb);
	else
		return (nb);
}
